name: Create envfile

on: [ push ]

jobs:

  create-envfile:
 
    runs-on: ubuntu-latest
 
    steps:
    - name: Make envfile
      uses: SpicyPizza/create-envfile@v2.0
      with:
        envkey_SOME_API_KEY: ${{ secrets.BASE_URL }}
        envkey_SECRET_KEY: ${{ secrets.API_SUFFIX }}
        
